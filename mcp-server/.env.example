# Auth (legacy API key)
MCP_API_KEY=your_shared_key

# OAuth 2.0 (for Claude.ai connector)
OAUTH_CLIENT_ID=reach-out-client
OAUTH_CLIENT_SECRET=change-this-secret
OAUTH_REDIRECT_URIS=https://claude.ai/oauth/callback
BASE_URL=https://reach-out-databse.vercel.app

# Data storage
DATA_DIR=../data
MCP_PORT=3333

# Optional GitHub sync
GITHUB_TOKEN=
GITHUB_OWNER=LiamFuller07
GITHUB_REPO=reach-out-databse
GITHUB_BRANCH=main
ENABLE_GITHUB_SYNC=true

# Optional Vercel deploy hook
VERCEL_DEPLOY_HOOK_URL=
AUTO_DEPLOY_ON_WRITE=true

# Optional throttling (milliseconds)
SYNC_DEBOUNCE_MS=8000
DEPLOY_MIN_INTERVAL_MS=60000
